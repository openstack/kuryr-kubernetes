============
Installation
============

At the command line::

    $ pip install kuryr-kubernetes

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv kuryr-kubernetes
    $ pip install kuryr-kubernetes
